const observer = new MutationObserver(() => {
  if (location.hash !== '#system' || document.querySelector('#authorized-users-panel')) return;
  const view=document.querySelector('#view'); if(!view) return;
  const panel=document.createElement('section'); panel.id='authorized-users-panel'; panel.className='panel section-gap';
  panel.innerHTML='<div class="panel-head"><h2>Authorized Users</h2></div><div class="panel-body"><p>Owner-managed access. Passwords are set privately by each invited user.</p><form id="authorized-user-form"><label>Name<input name="display_name" required></label><label>Email<input name="email" type="email" required></label><label>Role<select name="role"><option value="developer">Developer</option><option value="staff">Staff</option><option value="authorized_user">Authorized user</option></select></label><button class="gold" type="submit">Invite user</button><p id="authorized-user-status" role="status"></p></form><div id="authorized-user-list"></div></div>';
  view.append(panel);
  const list=panel.querySelector('#authorized-user-list'),status=panel.querySelector('#authorized-user-status');
  const load=async()=>{const response=await fetch('/.netlify/functions/authorized-users',{credentials:'same-origin'});if(!response.ok){status.textContent='Authorized-user management requires owner access.';return;}const result=await response.json();list.innerHTML=(result.users||[]).map(user=>`<div class="attention-item"><span><strong>${user.display_name}</strong><small>${user.email} · ${user.role} · ${user.active?'Active':'Revoked'}</small></span>${user.role!=='owner'?`<button type="button" data-user-id="${user.id}" data-user-action="${user.active?'deactivate':'activate'}">${user.active?'Revoke':'Reactivate'}</button>`:''}</div>`).join('')||'<p class="muted">No authorized users yet.</p>';};
  panel.querySelector('#authorized-user-form').addEventListener('submit',async event=>{event.preventDefault();const form=event.currentTarget;const body=Object.fromEntries(new FormData(form));const response=await fetch('/.netlify/functions/authorized-users',{method:'POST',credentials:'same-origin',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});let result={};try{result=await response.json();}catch{}status.textContent=response.ok&&result.invitation_sent?'Invitation sent.':result.error||'User could not be invited.';if(response.ok&&result.invitation_sent){form.reset();await load();}});
  panel.addEventListener('click',async event=>{const button=event.target.closest('[data-user-id]');if(!button)return;await fetch('/.netlify/functions/authorized-users',{method:'PATCH',credentials:'same-origin',headers:{'Content-Type':'application/json'},body:JSON.stringify({id:button.dataset.userId,action:button.dataset.userAction})});load();});
  load();
});
observer.observe(document.documentElement,{childList:true,subtree:true});
