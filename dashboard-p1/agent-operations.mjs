export function agentOperations(E,badge){
  const cards=[
    ['Website Agent','Public website builder','Release ready','Open blockers: 0'],
    ['Dashboard Agent','Practitioner dashboard builder','Release ready','Pending approvals: 0'],
    ['Manager Agent','Integration and release authority','Release authority','Open blockers: 0']
  ];
  return `<p class="view-intro">Read-only coordination summary for the three-agent development architecture. Production release authority remains with Manager.</p><div class="system-cards agent-operations-cards">${cards.map(([name,detail,role,queue])=>`<article class="panel panel-body"><div class="title-line"><h2>${E(name)}</h2>${badge(['Healthy','green'])}</div><p>${E(detail)}</p><div class="definition"><dt>Role</dt><dd>${E(role)}</dd><dt>Queue</dt><dd>${E(queue)}</dd></div><p class="muted">No production changes are initiated from this view.</p></article>`).join('')}</div><div class="grid section-gap"><section class="panel"><div class="panel-head"><h2>Recent handoffs</h2></div><div class="panel-body"><p class="muted">Agent handoff summaries will appear here after Manager integration.</p><ul class="timeline"><li><strong>Catalog route</strong><small>Website and Manager coordination · Completed</small></li><li><strong>Booking link alignment</strong><small>Website → Manager · Completed</small></li></ul></div></section><section class="panel"><div class="panel-head"><h2>Daron Status</h2></div><div class="panel-body"><p><span class="badge green">Operational</span></p><p>Public booking route and canonical service catalog are aligned.</p><div class="definition"><dt>Pending approvals</dt><dd>0</dd><dt>Open blockers</dt><dd>0</dd><dt>Release policy</dt><dd>Manager approval required</dd></div></div></section></div>`;
}
